name: firebase_basics
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^3.0.1
  firebase_auth: ^3.8.0
  cloud_firestore: ^3.4.7
  firebase_analytics: ^9.3.5
  number_selector: ^1.0.3
  firebase_messaging: ^13.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
